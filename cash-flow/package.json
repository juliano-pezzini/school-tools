{
  "name": "cash-flow",
  "version": "0.1.0",
  "private": true,
  "description": "Fluxo de Caixa — lógica pura testável (Vitest) + cola Apps Script",
  "scripts": {
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
